export { AEATInput, TaxpayerInfo, IncomeFromWork, CapitalIncome, PropertyIncome, Deductions, TaxId, TaxYear } from './types';
export { validateAEATInput, assertValidAEATInput, ValidationResult, ValidationError } from './validator';
